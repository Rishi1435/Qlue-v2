gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Base64 of the keystore, generated only to paste into the ANDROID_KEYSTORE_BASE64
# CI secret. It IS the signing key in another form — never commit it.
*.jks.base64.txt